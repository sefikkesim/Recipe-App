import React from 'react'
import {LoginContainer, StyledImg} from "./LoginStyle"

const Login = () => {
    return (
      <LoginContainer>
        <StyledImg/>
      </LoginContainer>
    );
}

export default Login
